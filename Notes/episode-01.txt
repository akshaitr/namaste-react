React comes with the philosophy of manupilating DOM nodes with JavaScript

`React.createElement` creates a JavaScript object

`ReactDOM.createRoot` creates the root element

`render` takes the object, create the h1 tag that browser understands and put it inside the root element

JSX will make our life easy when we have to create tags





Assignment:
-----------
● What is Emmet?
● Difference between a Library and Framework?
● What is CDN? Why do we use it?
● Why is React known as React?
● What is crossorigin in script tag?
● What is diference between React and ReactDOM
● What is difference between react.development.js and react.production.js files via CDN?
● What is async and defer? 